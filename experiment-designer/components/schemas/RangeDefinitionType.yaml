type: string
description: >
  The range definition type as generated by the external range definition
  platform:
    * `NetSpec` - The range definition was derived from a SimSpace NetSpec.
    * `CloneSource` - The range definition was derived from a SimSpace Clone Source.
    * `RunningRange` - The range definition was derived from an unspecified range definition type, and is assumed
  to be already deployed (Stack Optimization will not attempt deployment).
enum:
  - NetSpec
  - CloneSource
  - RunningRange
